{
  "name": "signwise-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0",
    "mammoth": "^1.8.0",
    "pdf-parse": "^1.1.1",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@types/pdf-parse": "^1.1.4",
    "typescript": "~5.8.2"
  }
}
